#
# Legacy root application controller.
# AppCtrl currently contains no active controller logic.
# Keep this module only while any remaining top-level AppCtrl usage exists
# in the AngularJS app shell.
#

angular.module('doubtfire.config.root-controller', [])

.controller("AppCtrl", ->
)